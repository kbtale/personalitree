[project]
name = "personalitree"
version = "0.1.0"
description = "Discover a person's accounts from one seed username, scrape them, and score every loaded instrument with an LLM."
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.43,<1.0",
    "cryptography>=43.0,<51.0",
    "django>=5.1,<5.2",
    "django-q2>=1.7,<2.0",
    "google-generativeai>=0.8,<1.0",
    "gunicorn>=22.0,<23.0",
    "lxml>=5.0,<6.0",
    "openai>=1.60,<2.0",
    "playwright>=1.49,<2.0",
    "playwright-stealth>=1.0,<2.0",
    "psycopg2-binary>=2.9,<3.0",
    "python-dotenv>=1.0,<2.0",
    "setuptools>=70.0,<81.0",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "black>=25.1.0",
    "pytest>=9.0.3",
    "pytest-cov>=6.0.0",
    "pytest-django>=4.9.0",
    "ruff>=0.12.0",
]

[tool.black]
line-length = 88
target-version = ["py312"]
extend-exclude = '''
/(
    \.venv
  | __pycache__
  | migrations
)/
'''

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "personalitree.settings"
pythonpath = ["."]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = [
    "-ra",
    "--strict-markers",
    "--showlocals",
    "--tb=short",
    "--cov=core",
    "--cov-branch",
    "--cov-report=term-missing",
]
markers = [
    "slow: tests that take more than a second",
]

[tool.ruff]
line-length = 88
target-version = "py312"
extend-exclude = [".venv", "__pycache__", "**/migrations", "build", "dist"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"
docstring-code-format = true

[tool.ruff.lint]
select = [
    "E",
    "W",
    "F",
    "I",
    "N",
    "UP",
    "B",
    "RUF",
    "BLE001",
    "C901",
    "PLR0911",
    "PLR0912",
    "PLR0913",
    "PLR1704",
    "PLR2004",
    "S314",
    "TRY400",
]

[tool.ruff.lint.isort]
known-first-party = ["core", "personalitree"]
force-sort-within-sections = true
combine-as-imports = true

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["PLR2004"]

[tool.coverage.run]
source = ["core"]
branch = true
omit = ["core/migrations/*"]

[tool.coverage.report]
exclude_also = [
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
]
